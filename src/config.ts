import PACKAGE_JSON from "../package.json";

export const APP_NAME = PACKAGE_JSON.name;
export const APP_VERSION = PACKAGE_JSON.version;

export const SHOW_EULA = true;
export const SHOW_CHANGELOG = true;
